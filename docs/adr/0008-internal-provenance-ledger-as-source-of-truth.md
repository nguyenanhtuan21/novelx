# Internal provenance ledger as source of truth

NovelX uses an internal append-only, queryable provenance ledger as the source of truth for chapter text and AI workflow artifacts. External content-credential metadata such as C2PA may supplement media assets, but it does not replace the lineage needed for prompts, models, datasets, reviewers, evaluations, revisions, approvals, and publishing history.
