# Lightweight monorepo with single-context domain docs

NovelX will start as a lightweight monorepo with separate application folders such as `apps/web`, `apps/api`, `apps/ai-factory`, and shared packages such as `packages/shared`. The domain documentation remains single-context at the repo root until the codebase and organization genuinely need multiple context glossaries, avoiding premature repo splitting or empty bounded-context documentation.
